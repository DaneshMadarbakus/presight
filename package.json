{
  "name": "presight",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "demo": "npm run install:all && npm run build:server && npm run dev",
    "dev": "concurrently \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=shared && npm run build --workspace=client && npm run build --workspace=server",
    "build:server": "npm run build --workspace=server",
    "clean": "rm -rf node_modules client/node_modules server/node_modules shared/node_modules",
    "install:all": "npm install && npm install --workspace=client && npm install --workspace=server && npm install --workspace=shared"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "typescript": "^5.9.2"
  }
}
